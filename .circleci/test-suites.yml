name: ci tests
discover: npx jest --listTests
run: mkdir -p test-reports && JEST_JUNIT_OUTPUT_FILE="<< outputs.junit >>" npx react-scripts test --runInBand --reporters=default --reporters=jest-junit --bail --watchAll=false << test.atoms >>
outputs:
  junit: test-reports/junit.xml
